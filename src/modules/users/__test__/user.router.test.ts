//get request to get all users Unauthed
// describe("user routes endpoints", () => {
//   it("GET /api/v1/users with unauthed agent will throw error", async () => {
//     const response = await unAuthedRequest.get("/api/v1/users");
//     expect(response.statusCode).toBe(401);
//   });
// });

//get request to get all users with auth
// it("GET /api/v1/users with authed agent will return array of users" , async ()=>{
//     const pageQ=2;
//     const limitQ=5;
//     const response=await
// })
